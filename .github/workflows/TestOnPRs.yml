name: Test on PRs

on:
  pull_request:
    branches:
      - main
    paths:
      - "src/**"
      - "test/**"
      - "*.toml"
    types: [opened, synchronize, reopened, labeled]

concurrency:
  # Skip intermediate builds: always.
  # Cancel intermediate builds: only if it is a pull request build.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

jobs:
  test:
    uses: ./.github/workflows/ReusableTest.yml
    with:
      os: ${{ matrix.os }}
      version: ${{ matrix.version }}
      arch: x64
      allow_failure: false
      run_codecov: ${{ matrix.os == 'ubuntu-latest' && matrix.version == '1' }}
    secrets:
      codecov_token: ${{ secrets.CODECOV_TOKEN }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macOS-latest
          - windows-latest
        version:
          - "1"
        has_test_all_label:
          - ${{ contains(github.event.pull_request.labels.*.name, 'pr-test-all') }}
        exclude: # If there is no label (`false`), the other OSs are excluded. If there is label (`true`), exclude doesn't match anything
          - has_test_all_label: false
            os: macOS-latest
          - has_test_all_label: false
            os: windows-latest
        include: # Always test the Julia LTS version on Ubuntu, regardless of the label
          - os: ubuntu-latest
            version: "lts"
